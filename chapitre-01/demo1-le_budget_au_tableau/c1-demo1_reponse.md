Apres un test sur les 20 ms disponibles, les cinq étapes prennent déjà 18 ms. Il ne reste donc que 2 ms pour le code.
Le code peut suffir pour 2ms mais c’est très peu de temps.
Si le code prend plus de 2ms, il dépasse le temps disponible. Il peut y avoir un retard dans l’affichage.
